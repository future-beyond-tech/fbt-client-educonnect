[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
startCommand = "dotnet EduConnect.Api.dll"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
